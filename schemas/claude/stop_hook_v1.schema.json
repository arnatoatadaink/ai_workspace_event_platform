{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "stop_hook_v1",
  "title": "Claude Stop Hook Payload",
  "description": "Payload delivered to the Stop hook when a Claude session ends.",
  "type": "object",
  "required": ["hook_event_name", "session_id"],
  "properties": {
    "hook_event_name": {
      "type": "string",
      "const": "Stop"
    },
    "session_id": {
      "type": "string",
      "description": "Unique identifier for the Claude session."
    },
    "stop_reason": {
      "type": "string",
      "enum": ["end_turn", "max_tokens", "stop_sequence", "tool_use"],
      "description": "Why the session stopped."
    },
    "transcript_path": {
      "type": "string",
      "description": "Absolute path to the session transcript JSONL file."
    }
  },
  "additionalProperties": true
}
